spring:
  application:
    name: Household-ledger
  datasource:
    url: jdbc:postgresql://localhost:5432/mydb
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver

  thymeleaf:
    cache: false   # 개발 중엔 false로 해서 새로고침만으로 화면 반영

  # 선택사항: Gmail 발송
  mail:
    host: smtp.gmail.com
    port: 587
    username: kcy1515@gmail.com
    password: hcec zxdo iycw ayzx   # 구글 계정 2단계 인증 후 "앱 비밀번호" 발급 필요
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
server:
  port: 8080

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true   # DB는 snake_case, Java는 camelCase 자동 매핑
  type-aliases-package: com.example.demo.domain